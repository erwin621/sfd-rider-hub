# Copy this file to .env and fill in real values.
# .env must NEVER be committed — add it to .gitignore if it isn't already.

# ── Server ──────────────────────────────────────────────
PORT=3000

# ── Supabase ────────────────────────────────────────────
# Service Role key (NOT the anon/public key) — this server is a
# trusted backend, so it bypasses RLS directly. Find it under
# Project Settings → API → service_role in the Supabase dashboard.
# Never send this key to the browser.
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ── Twilio Verify (SMS OTP) ─────────────────────────────
# Find these under Twilio Console → Account → API keys & tokens,
# and Verify → Services (create a Verify Service if you haven't).
# If left blank, the server falls back to a local DEV-mode OTP
# (the code is returned in the API response instead of texted) so
# you can keep developing without burning real SMS sends.
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxx
TWILIO_VERIFY_SERVICE_SID=VAxxxxxxxxxxxxxxxx
